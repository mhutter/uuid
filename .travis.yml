---
language: go
go:
  - stable

install: GO111MODULE=on go build -o uuid .

script:
  - test 1 -eq "$(./uuid | wc -l)"
  - test 37 -eq "$(./uuid | wc -c)"
  - test 5 -eq "$(./uuid -n 5 | wc -l)"
  - test 185 -eq "$(./uuid -n 5 | wc -c)"
